---
import { Image } from 'astro:assets';
import portrait from '../assets/portrait.jpg';
import { SITE } from '../lib/site';

interface Props {
	/** The page's largest picture loads first; use `lazy` for anything lower down. */
	loading?: 'eager' | 'lazy';
}
const { loading = 'eager' } = Astro.props;
---

<figure class="portrait">
	<Image
		src={portrait}
		alt={SITE.name}
		widths={[320, 480, 640]}
		sizes="(min-width: 52rem) 20rem, 9rem"
		loading={loading}
		fetchpriority={loading === 'eager' ? 'high' : undefined}
		decoding="async"
	/>
</figure>

<style>
	.portrait {
		margin: 0;
		/* Small on phones and tablets so it takes little room; larger beside the text on wide screens. */
		width: min(100%, 7.5rem);
		border-radius: var(--radius);
		overflow: hidden;
		background: var(--accent-soft);
		box-shadow: var(--shadow);
		outline: 1px solid var(--line);
		outline-offset: -1px;
	}
	@media (min-width: 40rem) {
		.portrait {
			width: min(100%, 9rem);
		}
	}
	@media (min-width: 52rem) {
		.portrait {
			width: min(100%, 18rem);
		}
	}
	.portrait :global(img) {
		display: block;
		width: 100%;
		height: auto;
		aspect-ratio: 3 / 4;
		object-fit: cover;
		object-position: 50% 18%;
	}
</style>
